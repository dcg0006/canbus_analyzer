// 09/07/16 Github test file
#include <iostream>
using namespace std;
int main(){
	cout << "hello Github" << endl;
	cout << "I'd like a few functions to be in our final project: \n";
	cout << "\t1: displayCaptureWindow1( filename ); //display one file in 1st window\n";
	cout << "\t2: displayCaptureWindow2( filename ); //display file in 2nd window\n";
	cout << "\t3: filterOutNonUnique(); //filters out all ID's that are present in both displays\n";
	cout << "\t4: addContentsToMainCollection( filename ); //adds any new unique IDs to the main collection if not already there\n";
	cout << "\t5: displayUnknownIDs( filename ); //displays IDs in file that are not defined or present in main collection\n"
	return 0;
}
//end file
